export default {
  saveReview(review) {
    console.log(review)
    // return new Promise((resolve, reject) => {
    //   axios.get(`/webapi/reviews?page=${page}`, {params}).then(res => {
    //     resolve(res)
    //   })
    // })
  }
}
